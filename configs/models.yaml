tier_1:
  provider_order:
    - transparent_synthetic_simulator
  max_input_chars: 120000
  max_sample_size: 500
  use_for:
    - protocol_extraction_heuristics
    - participant_simulation
    - sensitivity_analysis

disabled:
  - cloudflare_workers_ai
  - paid_api_fallback
  - gpu_inference
